# 博物通后端 - Cursor Rules

## 项目概述
- **名称**: Museum Guide Backend
- **框架**: FastAPI + Poetry
- **功能**: 图像识别 (GPT-4o Vision) + 语音合成 (OpenAI TTS)

## 技术栈
- Python 3.10+
- FastAPI
- OpenAI API (GPT-4o, TTS)
- Poetry (依赖管理)
- Uvicorn (ASGI 服务器)
- Task (任务自动化)

## 项目结构
```
backend/
├── app.py                  # 应用入口
├── routes.py               # 路由汇总
├── routers/
│   ├── analyze.py          # 图像识别 API
│   └── tts.py             # TTS API
├── src/
│   └── services/           # 业务逻辑
├── middleware/             # 中间件
├── utils/                 # 工具函数
├── tests/                 # 测试
└── Taskfile.yml           # 任务自动化
```

## 常用命令 (Taskfile)

```bash
cd backend

# 安装依赖
task install

# 启动开发服务器
task serve

# 运行测试
task test

# 代码格式化
task fmt

# 代码检查
task lint
```

### Taskfile 任务说明

| 任务 | 说明 |
|------|------|
| `task install` | poetry install - 安装依赖 |
| `task serve` | uvicorn 启动服务 (端口 8000) |
| `task test` | pytest 运行测试 |
| `task fmt` | isort + autoflake + black 格式化代码 |
| `task lint` | ruff 检查代码 |
| `task isort` | isort 排序 import |
| `task autoflake` | autoflake 移除未使用 import |
| `task black` | black 格式化代码 |

## 代码规范

### Python
- 使用 type hints
- 函数 docstring 用中文
- 异步用 async/await
- 错误处理用 try/except

### API 设计
```python
# 示例路由
@app.post("/analyze/")
async def analyze_image(file: UploadFile):
    """分析图片"""
    # ...
    return {"text": "识别结果"}
```

### 格式化 (task fmt)
格式化会自动执行以下步骤：
1. **isort** - 排序 import 语句
2. **autoflake** - 移除未使用的 import 和变量
3. **black** - 格式化代码风格

运行格式化的意义：
- 保持代码风格一致
- 减少代码审查负担
- 自动修复简单问题

### 常量配置
- 环境变量放 `.env`
- 配置用 `pydantic`

## 环境变量 (.env)
```
OPENAI_API_KEY=sk-xxx
```

## 约束
- 不使用 Django
- 优先用 Python 3.10+ 特性
- API 响应用 Pydantic 模型
- 提交前运行 `task fmt` 格式化代码
